/**
 * DEPRECATED (pivot 2026-07-11): game câu cá không có khái niệm "chết"/hồi sinh — người chơi luôn
 * sống, chỉ chuyển qua lại giữa các trạng thái câu cá (xem FishingState trong @bomio/shared và
 * backend/src/systems/fishing.ts). Không còn được import ở đâu. Giữ lại file rỗng thay vì xoá do
 * giới hạn quyền xoá file trong thư mục workspace — có thể xoá thủ công nếu muốn dọn sạch.
 */
export {};
