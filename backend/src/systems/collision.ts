/**
 * DEPRECATED (pivot 2026-07-11): va chạm vật lý giữa người chơi thuộc game PvP cũ — không cần
 * thiết trong game câu cá (người chơi đứng yên quanh hồ, không đi xuyên/đè lên nhau theo cách cần
 * xử lý va chạm riêng). Không còn được import ở đâu. Giữ lại file rỗng thay vì xoá do giới hạn
 * quyền xoá file trong thư mục workspace — có thể xoá thủ công nếu muốn dọn sạch.
 */
export {};
