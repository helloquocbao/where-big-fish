/**
 * DEPRECATED (pivot 2026-07-11): thuộc về game PvP/growth cũ (hạt năng lượng để ăn lớn dần) — đã
 * bỏ hoàn toàn trong game câu cá. Không còn được import ở đâu (xem GameRoom.ts, systems/fishing.ts
 * cho cơ chế mới). Giữ lại file rỗng thay vì xoá do giới hạn quyền xoá file trong thư mục workspace
 * — có thể xoá thủ công nếu muốn dọn sạch.
 */
export {};
